<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Каталог продуктов</title>
    <link
      rel="stylesheet"
      href="/styles.css"
      type="text/css"
    />
  </head>
  <body>
    <div class="title-page">
      <div class="header">
        <h1>Каталог продуктов</h1>
      </div>
      <%- include('navbar.ejs', {categories: locals.categories}) %>
      <div class="content">
        <h2>Доступные товары по вашему запросу:</h2>
        <div class="groceries">
          <% if(locals.groceries) {%>
            <% groceries.forEach(grocery => { %>
              <%- include('grocery_card.ejs', {grocery}) %>
            <% })%>
          <% }%>
          <a href="/form" class="new-grocery-card">
            <h3>Новая позиция</h3>
            <button class="new-grocery-card-btn" type="button">+</button>
          </a>
        </div>
      </div>
      <div class="footer">
        <p>
          Контакты: info@example.com<br />
          Поддержка: support@example.com<br />
          Социальные сети:
          <a href="#">Telegram</a>,
          <a href="https://www.vk.com/example">ВКонтакте</a>
        </p>
      </div>
    </div>
  </body>
</html>
